apiVersion: cilium.io/v1alpha1
kind: TracingPolicy
metadata:
  name: dos-accept-detect
spec:
  tracepoints:
  - subsystem: syscalls
    event: sys_enter_accept
    selectors:
    - matchActions:
      - action: Post